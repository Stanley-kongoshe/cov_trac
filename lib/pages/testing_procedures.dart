import 'package:flutter/material.dart';

class TestingProcedures extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}
